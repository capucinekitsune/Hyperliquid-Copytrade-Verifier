# Core runtime dependencies for the Hyperliquid Copy-Trade Verifier TUI.
# Pin to compatible-release ranges; exact resolution happens via pyproject.toml / uv.lock.

# --- TUI framework ---
textual>=0.86.0,<2.0.0
rich>=13.9.0,<14.0.0

# --- Data / numerics (verification engine) ---
numpy>=2.1.0,<3.0.0
pandas>=2.2.0,<3.0.0

# --- Networking (read-only public Hyperliquid API) ---
httpx>=0.27.0,<1.0.0
websockets>=13.0,<14.0

# --- Configuration / serialization ---
tomli>=2.0.1; python_version < "3.11"
tomli-w>=1.0.0
pydantic>=2.9.0,<3.0.0

# --- Utilities ---
click>=8.1.7,<9.0.0
platformdirs>=4.3.0,<5.0.0
